import React from 'react';
import {Box} from "@mui/material";

const OnlyJavaTalk = () => {
    return (
        <Box
            sx={{

                color: 'blue',
                fontSize: '14px',
                fontWeight: 'bold',
                // position: 'absolute',
                // right: '10px',

                }}
        >
            <h1>Only Java talk!</h1>
        </Box>
    );
};

export default OnlyJavaTalk;